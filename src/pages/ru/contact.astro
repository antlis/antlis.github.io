---
import Layout from '/src/layouts/Layout.astro'
import Socials from '/src/components/Socials.astro'
import { t } from '/src/i18n/utils'

const i = t('ru')
---

<Layout title={i.contact.title} description={i.contact.description} pageType="contact">
  <section class="contact">
    <h1>{i.contact.heading}</h1>
    <p>{i.contact.text}</p>

    <div class="contact-links">
      <a href="mailto:antlis@protonmail.com" class="email">antlis@protonmail.com</a>
      <Socials />
    </div>
  </section>
</Layout>

<style>
  .contact {
    max-width: 640px;
    margin: 0 auto;
    padding-top: 2rem;
  }

  .contact h1 {
    margin-bottom: 1.5rem;
  }

  .contact p {
    font-size: clamp(1rem, 0.95rem + 0.25vw, 1.1rem);
    line-height: 1.75;
    margin-bottom: 2rem;
  }

  .contact-links {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .email {
    font-size: clamp(0.95rem, 0.9rem + 0.25vw, 1.1rem);
    font-weight: 500;
    color: var(--accent);
    word-break: break-all;
  }

  .email:hover {
    color: var(--accent-hover);
  }
</style>
