---
import ProjectCard from '/src/components/ProjectCard.astro'
import { getCollection } from 'astro:content'
import type { CollectionEntry } from 'astro:content'

interface Props {
  post: CollectionEntry<'projects'>
}

export async function getStaticPaths() {
  const posts = await getCollection('projects', ({ data }) => data.locale === 'ru')

  return posts.map((post) => ({
    params: { slug: post.slug.replace(/-ru$/, '') },
    props: { post },
  }))
}

const { post } = Astro.props
const projects = await getCollection('projects', ({ data }) => data.locale === 'ru')
const { Content } = await post.render()
---

<ProjectCard post={post} projects={projects} locale="ru">
  <Content />
</ProjectCard>
