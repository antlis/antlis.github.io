---
interface Props {
	title: string;
	body?: string;
	description?: string;
	href: string;
  imgSrc?: string;
  imgAlt?: string;
  category?: string;
  tags?: string[];
  date?: string;
}

const { href, title, body, description, imgAlt, imgSrc, category, tags, date } = Astro.props;
const displayText = body || description || '';
---

<a href={href} class:list={["card", { "card-with-img": !!imgSrc }]} data-tags={tags?.join(',') || ''} data-category={category || ''}>
  <div class="card-content">
    <h3>{title}</h3>
    {date && <time class="card-date"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>{date}</time>}
    {displayText && <p>{displayText}</p>}
    {tags && tags.length > 0 && (
      <div class="card-tags">
        {tags.map((tag) => (
          <span class="card-tag" data-tag={tag}>{tag}</span>
        ))}
      </div>
    )}
  </div>
  {imgSrc && (
    <div class="card-img">
      <img
        src={imgSrc}
        alt={imgAlt || title}
        width={200}
        height={200}
        loading="lazy"
        decoding="async"
      />
    </div>
  )}
</a>

<style>
  .card {
    display: block;
    padding: 1.25rem;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--card-bg);
    text-decoration: none;
    transition: background 0.2s, border-color 0.2s;
  }

  .card-with-img {
    display: flex;
    align-items: center;
    padding: 0;
    overflow: hidden;
  }

  .card-with-img .card-content {
    flex: 1;
    min-width: 0;
    padding: 1.25rem;
  }

  .card:hover {
    background: var(--card-hover);
    border-color: var(--text-secondary);
  }

  .card h3 {
    margin: 0 0 0.5rem;
    font-size: 1rem;
    font-weight: 600;
  }

  .card-date {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    margin: -0.25rem 0 0.5rem;
    font-size: 0.75rem;
    color: var(--text-secondary);
  }

  .card-date svg {
    flex-shrink: 0;
  }

  .card p {
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--text-secondary);
  }

  .card-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-top: 0.75rem;
  }

  .card-tag {
    font-size: 0.7rem;
    padding: 0.15rem 0.5rem;
    background: var(--card-hover);
    color: var(--text-secondary);
    border: 1px solid var(--border);
    border-radius: 999px;
  }

  .card-img {
    flex-shrink: 0;
    width: 180px;
    aspect-ratio: 16 / 10;
    margin: 1rem 1rem 1rem 0;
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--card-hover);
  }

  .card-img :global(img) {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
  }

  @media (max-width: 480px) {
    .card-with-img {
      flex-direction: column-reverse;
    }

    .card-img {
      width: 100%;
      aspect-ratio: 16 / 10;
      margin: 0;
      border: 0;
      border-bottom: 1px solid var(--border);
      border-radius: 0;
    }
  }
</style>
